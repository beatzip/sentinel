[package]
name = "sentinel-source2"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Source 2 demo adapter using source2-demo for CS2"

[dependencies]
sentinel-core = { path = "../sentinel-core" }
sentinel-common = { path = "../sentinel-common" }
source2-demo = { version = "0.5", features = ["cs2"] }
