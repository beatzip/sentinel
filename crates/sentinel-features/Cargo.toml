[package]
name = "sentinel-features"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Feature extraction engine with 100+ independent features"

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
sentinel-core = { path = "../sentinel-core" }
sentinel-common = { path = "../sentinel-common" }
sentinel-world = { path = "../sentinel-world" }
sentinel-visibility = { path = "../sentinel-visibility" }
